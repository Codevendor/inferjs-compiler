'use strict';

import { setValue } from "../helpers/helpers.js";

/**
 * Parses the tag @abstract
 * @param {object} parser - The parser class.
 * @param {string} filePath - The filepath of where the line exists.
 * @param {string} inferid = The inferid for the comment.
 * @param {object} lineObject - The lineObject to parse.
 */
export function tagAbstract(parser, filePath, inferid, lineObject) {

    // Set Param Name
    //setValue(parser.source, ['methods', 'infers', inferid, lineObject.tag], true);

}